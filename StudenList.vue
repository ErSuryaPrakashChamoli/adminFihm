<template>
    <div>
      <h1>Hello, World!</h1>
      <p>This is a Vue.js component.</p>
    </div>
  </template>

<script>
  export default {
    name: 'MyComponent', // Optional: Give the component a unique name
    data() {
      return {
        // Data properties
      };
    },
    methods: {
      // Methods for handling events or logic
    },
    computed: {
      // Computed properties
    },
    watch: {
      // Watchers to react to data changes
    },
    created() {
      // Lifecycle hook called after the component is created
    },
    mounted() {
      // Lifecycle hook called after the component is mounted to the DOM
    },
  };
</script>